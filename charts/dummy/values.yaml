serverName: minigame-server
image:
  repository: itzg/minecraft-server
  tag: latest
  pullPolicy: IfNotPresent

server:
  ipBlock: 99.226.105.1/32

env:
  EULA: "TRUE"

service:
  type: NodePort
  nodePort: null  # Let K8s assign randomly
  port: 25565

extraPodSpec:
  hostNetwork: true
  dnsPolicy: ClusterFirstWithHostNet
